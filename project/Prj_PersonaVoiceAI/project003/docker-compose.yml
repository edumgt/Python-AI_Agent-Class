services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PROJECT_ID: ${PROJECT_ID:-project003}
      PROJECT_NAME: ${PROJECT_NAME:-사전 데이터 기반 PERSO AI의 답변 커스텀하기}
      PROJECT_TRACK: ${PROJECT_TRACK:-프로젝트-C}
      PROJECT_TOPIC: ${PROJECT_TOPIC:-사전 데이터 기반 PERSO AI의 답변 커스텀하기}
    ports:
      - "${APP_HOST_PORT:-9103}:8080"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=2).read()"]
      interval: 15s
      timeout: 5s
      retries: 5
    restart: unless-stopped
