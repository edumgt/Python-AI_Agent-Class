services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PROJECT_ID: ${PROJECT_ID:-project020}
      PROJECT_NAME: ${PROJECT_NAME:-Prj_PersonaVoiceAI}
      PROJECT_TRACK: ${PROJECT_TRACK:-프로젝트-4}
      PROJECT_TOPIC: ${PROJECT_TOPIC:-PERSONA AI 지속학습과 품질 운영 · 단계 5/5 운영 최적화}
    ports:
      - "${APP_HOST_PORT:-9120}:8080"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=2).read()"]
      interval: 15s
      timeout: 5s
      retries: 5
    restart: unless-stopped
