# 라인 1: 기존 설명 주석으로, 아래 코드의 의도나 맥락을 안내합니다.
# 이 파일은 www.edumgt.co.kr 의 에듀엠지티에 저작권이 있습니다

# 라인 2: 가독성을 위해 구분한 빈 줄입니다.

# 라인 3: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
"""class008 example2: 변수와 자료형"""

# 라인 4: 가독성을 위해 구분한 빈 줄입니다.

# 라인 5: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
TOPIC = "변수와 자료형"
# 라인 6: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
EXAMPLE_TEMPLATE = "variables"

# 라인 7: 가독성을 위해 구분한 빈 줄입니다.

# 라인 8: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def inspect_values():
    # 라인 9: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
    sample = {"name": "민수", "age": 11, "height": 140.5, "is_student": True}
    # 라인 10: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
    return {k: type(v).__name__ for k, v in sample.items()}

# 라인 11: 가독성을 위해 구분한 빈 줄입니다.

# 라인 12: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def main():
    # 라인 13: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("오늘 주제:", TOPIC)
    # 라인 14: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("자료형 확인:", inspect_values())

# 라인 15: 가독성을 위해 구분한 빈 줄입니다.


# 라인 16: 가독성을 위해 구분한 빈 줄입니다.

# 라인 17: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def extension_mission():
    # 라인 18: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
    return {
        # 라인 19: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
        "mission": "입력값을 바꿔 2가지 이상 결과를 비교하기",
        # 라인 20: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
        "check": "결과 차이를 한 줄로 설명하기",
    # 라인 21: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
    }

# 라인 22: 가독성을 위해 구분한 빈 줄입니다.

# 라인 23: 파일을 직접 실행했을 때만 아래 실행 블록이 동작하도록 분기합니다.
if __name__ == "__main__":
    # 라인 24: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
    main()
    # 라인 25: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("확장 미션:", extension_mission())
