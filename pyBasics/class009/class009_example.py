# 라인 1: 기존 설명 주석으로, 아래 코드의 의도나 맥락을 안내합니다.
# 이 파일은 www.edumgt.co.kr 의 에듀엠지티에 저작권이 있습니다

# 라인 2: 가독성을 위해 구분한 빈 줄입니다.

# 라인 3: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
"""class009 example1: 연산자와 조건문"""

# 라인 4: 가독성을 위해 구분한 빈 줄입니다.

# 라인 5: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
TOPIC = "연산자와 조건문"
# 라인 6: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
EXAMPLE_TEMPLATE = "condition"

# 라인 7: 가독성을 위해 구분한 빈 줄입니다.

# 라인 8: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def grade(score):
    # 라인 9: 조건을 평가해 실행 흐름을 분기하는 조건문입니다.
    if score >= 90:
        # 라인 10: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
        return "A"
    # 라인 11: 조건을 평가해 실행 흐름을 분기하는 조건문입니다.
    if score >= 80:
        # 라인 12: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
        return "B"
    # 라인 13: 조건을 평가해 실행 흐름을 분기하는 조건문입니다.
    if score >= 70:
        # 라인 14: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
        return "C"
    # 라인 15: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
    return "D"

# 라인 16: 가독성을 위해 구분한 빈 줄입니다.

# 라인 17: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def main():
    # 라인 18: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
    score = 86
    # 라인 19: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("오늘 주제:", TOPIC)
    # 라인 20: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print(f"점수 {score} -> 등급 {grade(score)}")

# 라인 21: 가독성을 위해 구분한 빈 줄입니다.


# 라인 22: 가독성을 위해 구분한 빈 줄입니다.

# 라인 23: 파일을 직접 실행했을 때만 아래 실행 블록이 동작하도록 분기합니다.
if __name__ == "__main__":
    # 라인 24: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
    main()
