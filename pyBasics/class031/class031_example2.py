# 라인 1: 기존 설명 주석으로, 아래 코드의 의도나 맥락을 안내합니다.
# 이 파일은 www.edumgt.co.kr 의 에듀엠지티에 저작권이 있습니다

# 라인 2: 가독성을 위해 구분한 빈 줄입니다.

# 라인 3: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
"""class031 example2: 예외처리와 디버깅"""

# 라인 4: 가독성을 위해 구분한 빈 줄입니다.

# 라인 5: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
TOPIC = "예외처리와 디버깅"
# 라인 6: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
EXAMPLE_TEMPLATE = "exception"

# 라인 7: 가독성을 위해 구분한 빈 줄입니다.

# 라인 8: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def safe_divide(a, b):
    # 라인 9: 예외가 발생할 수 있는 코드를 안전하게 감싸는 블록 시작입니다.
    try:
        # 라인 10: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
        return a / b
    # 라인 11: 발생한 예외를 잡아 프로그램 중단을 방지하고 처리합니다.
    except ZeroDivisionError:
        # 라인 12: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
        return "0으로 나눌 수 없어요."

# 라인 13: 가독성을 위해 구분한 빈 줄입니다.

# 라인 14: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def main():
    # 라인 15: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("오늘 주제:", TOPIC)
    # 라인 16: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("10 / 2 =", safe_divide(10, 2))
    # 라인 17: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("10 / 0 =", safe_divide(10, 0))

# 라인 18: 가독성을 위해 구분한 빈 줄입니다.


# 라인 19: 가독성을 위해 구분한 빈 줄입니다.

# 라인 20: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def extension_mission():
    # 라인 21: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
    return {
        # 라인 22: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
        "mission": "입력값을 바꿔 2가지 이상 결과를 비교하기",
        # 라인 23: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
        "check": "결과 차이를 한 줄로 설명하기",
    # 라인 24: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
    }

# 라인 25: 가독성을 위해 구분한 빈 줄입니다.

# 라인 26: 파일을 직접 실행했을 때만 아래 실행 블록이 동작하도록 분기합니다.
if __name__ == "__main__":
    # 라인 27: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
    main()
    # 라인 28: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("확장 미션:", extension_mission())
