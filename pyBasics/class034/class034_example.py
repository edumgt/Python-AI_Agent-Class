# 라인 1: 기존 설명 주석으로, 아래 코드의 의도나 맥락을 안내합니다.
# 이 파일은 www.edumgt.co.kr 의 에듀엠지티에 저작권이 있습니다

# 라인 2: 가독성을 위해 구분한 빈 줄입니다.

# 라인 3: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
"""class034 example1: 객체지향 기초"""

# 라인 4: 가독성을 위해 구분한 빈 줄입니다.

# 라인 5: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
TOPIC = "객체지향 기초"
# 라인 6: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
EXAMPLE_TEMPLATE = "oop"

# 라인 7: 가독성을 위해 구분한 빈 줄입니다.

# 라인 8: 데이터와 동작을 묶기 위한 클래스 구조를 정의합니다.
class Student:
    # 라인 9: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
    def __init__(self, name, level):
        # 라인 10: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
        self.name = name
        # 라인 11: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
        self.level = level

# 라인 12: 가독성을 위해 구분한 빈 줄입니다.

    # 라인 13: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
    def introduce(self):
        # 라인 14: 함수 실행 결과를 호출한 위치로 반환하고 함수 실행을 종료합니다.
        return f"안녕하세요, 저는 {self.name}이고 {self.level} 단계예요."

# 라인 15: 가독성을 위해 구분한 빈 줄입니다.

# 라인 16: 재사용 가능한 함수 동작을 정의하는 함수 선언입니다.
def main():
    # 라인 17: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print("오늘 주제:", TOPIC)
    # 라인 18: 값을 변수에 저장하거나 갱신해 이후 로직에서 재사용합니다.
    student = Student("지유", "기초응용")
    # 라인 19: 계산 결과나 안내 메시지를 콘솔에 출력합니다.
    print(student.introduce())

# 라인 20: 가독성을 위해 구분한 빈 줄입니다.


# 라인 21: 가독성을 위해 구분한 빈 줄입니다.

# 라인 22: 파일을 직접 실행했을 때만 아래 실행 블록이 동작하도록 분기합니다.
if __name__ == "__main__":
    # 라인 23: 아래 코드 한 줄을 단계적으로 실행해 현재 학습 개념을 보여줍니다.
    main()
