# Mermaid Preview Test

If the diagram below renders in Markdown Preview, Mermaid support is working.

```mermaid
flowchart TD
  A[Start] --> B{Mermaid Enabled?}
  B -- Yes --> C[Rendered Diagram]
  B -- No --> D[Install/Enable Mermaid Support]
```
